spring:
  datasource:
    url: jdbc:postgresql://localhost:54320/test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  flyway:
    enabled: true